# Server-side key used by /api/generate when visitors use the live demo on your site.
OPENAI_API_KEY=

# After changing this file, restart the dev server: npm run dev
# If pages show 500 errors locally, run: npm run dev:clean
#
# Add the same key in Vercel: Project Settings → Environment Variables → OPENAI_API_KEY
