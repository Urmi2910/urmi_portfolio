Add your assets here:

- `profile.jpg` — your headshot (recommended ~800×1000px, portrait)
- `resume.pdf` — your resume for view/download links

Then update links in `src/data/portfolio.ts` if you use different filenames.
