Add your assets here:

- `profile.jpg` — your headshot (recommended ~800×1000px, portrait)
- `resume.pdf` — served for resume downloads (auto-synced from `Urmi-Shah-Resume.pdf` at project root on build)

To update the downloadable resume, replace `Urmi-Shah-Resume.pdf` in the project root and run:

```bash
npm run sync:resume
```
