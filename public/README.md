Add your assets here:

- `Urmi_Content Designer.pdf` — the file visitors download (auto-synced from the project root on build)

To update the downloadable resume, replace `Urmi_Content Designer.pdf` in the project root and run:

```bash
npm run sync:resume
```
