export function FaviconMark({ dimension }: { dimension: number }) {
  return (
    <div
      style={{
        width: dimension,
        height: dimension,
        borderRadius: "50%",
        background: "#dcd0ef",
      }}
    />
  );
}
